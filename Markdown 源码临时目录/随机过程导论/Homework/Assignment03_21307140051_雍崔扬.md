## 随机过程导论 Assignment 03

**姓名：** 雍崔扬  
**学号：** 21307140051  
**Due Date:**  June 20, 2024

### Problem 1

Let $\{B(t):t \geq 0\}$ be a standard Brownian motion.   
For $s \leq t$, determine the distribution of $B(s) + B(t)$.

**Solution:**    
对于任意 $s\leq t$ 都有联合分布：  
$\begin{bmatrix}
B(s)\\
B(t)-B(s)\end{bmatrix} \sim N(\begin{bmatrix}
0\\
0\end{bmatrix},\begin{bmatrix}
s&\\
&t-s\end{bmatrix})$  

因此我们有：
$$
\begin{align}
B(s) + B(t) 
&= 2B(s) + (B(t)-B(s))\\
&= 
\begin{bmatrix}
2\\
1\end{bmatrix}^T
\begin{bmatrix}
B(s)\\
B(t)-B(s)\end{bmatrix}\\
&\overset{d}=
N(\begin{bmatrix}
2\\
1\end{bmatrix}^T\begin{bmatrix}
0\\
0\end{bmatrix}, 
\begin{bmatrix}
2\\
1\end{bmatrix}^T 
\begin{bmatrix}
s &\\
&t-s\end{bmatrix}\begin{bmatrix}
2\\
1\end{bmatrix})\\
&= N(0,3s + t)\end{align}
$$
表明 $B(s)+B(t)\sim N(0,3s + t)$ 



### Problem 2

For $t \geq 0$, define $X(t) = Z\sqrt{t}$ where $Z \sim N(0, 1)$.

(a) What is the distribution of $X(t)$?

- **Solution:**    
  对于任意 $t\geq 0$，$X(t) = Z\sqrt{t}\sim N(0\cdot \sqrt{t},1\cdot (\sqrt{t})^2) = N(0,t)$ 

(b) Is $\{X(t): t \geq 0\}$ a Brownian motion? Why?

- **Lemma：(Brown 运动的第二种等价定义, 苏中根 定理 7.1)**  
  若 $\mathbf X=\{X(t):t\geq 0\}$ 是一个**实数值 Gauss 过程** (即任意有限维联合正态)，  
  且 $\begin{cases}
  \text{E}[X(t)] = 0\\
  \text{Cov}[X(s),X(t)] = s\and t = \min\{s,t\}\end{cases}$   
  则 $\mathbf X=\{X(t):t\geq 0\}$ 是**标准 Brown 运动**.

- **Solution:**  
  $\{X(t): t \geq 0\}$ 不是一个 Brown 运动.      
  我们说明它不满足 Brown 运动的第二个等价定义：

  - **正态过程：**  
    其任意有限维联合分布是多元正态的，  
    具体来说，任意给定 $n$ 个时刻 $t_1,t_2,\dots,t_n$   
    我们都有 $\begin{bmatrix}
    X(t_1)\\
    \vdots\\
    X(t_n)\end{bmatrix} = \begin{bmatrix}
    \sqrt{t_1}\\
    \vdots\\
    \sqrt{t_n}\end{bmatrix} \odot
    Z$   
    它是正态随机变量 $Z$ 的线性组合，因而服从多元正态分布.

  - **我们计算 $\mathbf X = \{X(t):t\geq 0\}$ 的均值函数和自相关函数：**   
    $\begin{cases}
    \text{E}[X(t)] = \text{E}[Z\sqrt{t}] \\
    \qquad\quad\ = 0\cdot \sqrt{t}\\ 
    \qquad\quad\ =0\quad (\forall\ t\geq 0)\\
    \text{Cov}(X(s),X(t)) = \text{E}[(X(s)-0)(X(t)-0)]\\
    \qquad\qquad\qquad\ \ \ \ = 
    \text{E}[Z\sqrt{s}\cdot Z\sqrt{t}]\\
    \qquad\qquad\qquad\ \ \ \ = 
    \sqrt{st}\cdot\text{E}[Z^2]\\
    \qquad\qquad\qquad\ \ \ \ = 
    \sqrt{st}\cdot (\text{Var}(Z) + (\text{E}[Z])^2)\\
    \qquad\qquad\qquad\ \ \ \ = 
    \sqrt{st}\cdot (1 + 0^2)\\
    \qquad\qquad\qquad\ \ \ \ = 
    \sqrt{st}\quad (\forall\ s,t\geq 0)\end{cases}$ 

    我们发现自相关函数 $\text{Cov}(X(s),X(t)) = \sqrt{st}\neq s\and t$   
    不满足 Brown 运动的第二种等价定义.

  综上所述，$\{X(t):t\geq 0\}$ 不是一个 Brown 运动.



### Problem 3

Let $\{W(t): t \geq 0\}$ and $\{B(t):t \geq 0\}$ be two independent standard Brownian motions.   
Let $\rho$ be a constant such that $-1 < \rho < 1$.   
Define the stochastic process $\{X(t): t \geq 0\}$ as $X(t) = \rho W(t) + \sqrt{1 - \rho^2} B(t)$

**(a)** Show that $\{X(t):t \geq 0\}$ is a standard Brownian motion.

**Solution:**  
我们通过标准 Brown 运动的第二种等价定义来证明：

- **正态过程：**    
  任意给定 $n$ 个时刻 $t_1,t_2,\dots,t_n$ 和 $c_1,c_2,\dots,c_n$   
  考虑其**线性组合** $\underset{i=1}{\overset{n}\sum}c_i X(t_i)$：  
  $\begin{align}
  \underset{i=1}{\overset{n}\sum}c_i X(t_i)
  &= 
  \sum_{i=1}^n c_i[\rho W(t_i) + \sqrt{1 - \rho^2} B(t_i)]\\
  &=
  \rho\sum_{i=1}^n c_i W(t_i) + \sqrt{1-\rho^2}\sum_{i=1}^n c_i B(t_i)\end{align}$  
  它作为正态随机变量 $\{W(t_i)\}_{i=1}^n$ 和 $\{B(t_i)\}_{i=1}^n$ 的线性组合，也是**正态随机变量**.  
  表明 $\mathbf X = \{X(t):t\geq 0\}$ 是 **Gauss 过程** (正态过程).
- **我们计算 $\mathbf X = \{X(t):t\geq 0\}$ 的均值函数和自相关函数：**  
  $\begin{cases}
  \text{E}[X(t)] = \text{E}[\rho W(t) + \sqrt{1 - \rho^2} B(t)] \\
  \qquad\quad\ = \rho\cdot 0-\sqrt{1-\rho^2}\cdot 0\\ 
  \qquad\quad\ =0\quad (\forall\ t\geq 0)\\
  \text{Cov}(X(s),X(t)) = \text{E}[(X(s)-0)(X(t)-0)]\\
  \qquad\qquad\qquad\ \ \ \ = 
  \text{E}[(\rho W(s) + \sqrt{1 - \rho^2} B(s))(\rho W(t) + \sqrt{1 - \rho^2} B(t))]\\
  \qquad\qquad\qquad\ \ \ \ = 
  \rho^2 \text{E}[W(s)W(t)] +\rho\sqrt{1-\rho^2} \text{E}[B(s)]\text{E}[W(t)] +
  \rho\sqrt{1-\rho^2} \text{E}[W(s)]\text{E}[B(t)] +(1-\rho^2)\text{E}[B(s)B(t)]\\
  \qquad\qquad\qquad\ \ \ \ = 
  \rho^2\cdot (s\and t) + \rho\sqrt{1-\rho^2}\cdot 0\cdot 0 + \rho\sqrt{1-\rho^2} \cdot 0\cdot 0 + (1-\rho^2)\cdot (s\and t)\\
  \qquad\qquad\qquad\ \ \ \ =  
  s\and t\quad (\forall\ s,t\geq 0)\end{cases}$

综上所述，$\{X(t):t \geq 0\}$ 是标准 Brown 运动.



**(b)** Determine the covariance and correlation coefficient of $X(t)$ and $W(t)$.

**Solution:**  

- 计算 $\text{Cov}(X(t),W(t))$：  
  $\begin{align}
  \text{Cov}(X(t),W(t))
  &= 
  \text{E}[(X(t)-0)(W(t)-0)]\\
  &=
  \text{E}[(\rho W(t) + \sqrt{1 - \rho^2} B(t)) W(t)]\\
  &=
  \rho\cdot\text{E}[(W(t))^2] + \sqrt{1-\rho^2}\cdot\text{E}[B(t)]\text{E}[W(t)]\\
  &=
  \rho\cdot t + \sqrt{1-\rho^2}\cdot 0\cdot 0\\
  &=
  \rho t\end{align}$
- 计算 $\text{Corr}(X(t),W(t))$：  
  $\begin{align}
  \text{Corr}(X(t),W(t))
  &=
  \frac{\text{Cov}(X(t),W(t))}{\sqrt{\text{Var}(X(t))\text{Var}(W(t))}}\\
  &=
  \frac{\rho t}{\sqrt{t\cdot t}}\\
  &= \rho\end{align}$



### Problem 4

Let $\{B(t):t \geq 0\}$ be a standard Brownian motion.

(a) Determine $P(B(2.1) > 1.7 | B(1.6) = 0.5)$.

- **Solution:**  
  对于任意 $s\leq t$ 都有条件分布 $(B(t)|B(s)=x) \overset{d}= N(x,t-s)$   
  因此 $(B(2.1)|B(1.6)=0.5)\overset{d}= N(0.5,0.5)$   
  $$
  \begin{align}
  \text{P}\{B(2.1)>1.7|B(1.6)=0.5\}
  &=
  \text{P}\{N(0.5,0.5)>1.7\}\\
  &=
  \text{P}\{N(0,1) > \frac{1.7-0.5}{\sqrt{0.5}}\}\\
  &=
  1-\Phi(1.2\sqrt{2})\\
  &\approx
  0.0455\end{align}
  $$

(b) Determine $P(B(1.6) \leq 0.5 | B(2.1) = 1.7)$.

- **Solution:**    
  对于任意 $s\leq t$ 都有条件分布 $(B(s)|B(t)=y) \overset{d}= N(\frac{s}{t}y,\frac{s}{t}(t-s))$   
  因此 $(B(1.6)|B(2.1)=1.7)\overset{d}= N(\frac{1.6}{2.1}1.7,\frac{1.6}{2.1}0.5)$    
  $$
  \begin{align}
  \text{P}\{B(1.6) \leq 0.5 | B(2.1) = 1.7\}
  &=
  \text{P}\{N(\frac{1.6}{2.1}1.7,\frac{1.6}{2.1}0.5)\leq 0.5\}\\
  &=
  \text{P}\{N(0,1) \leq \frac{0.5-\frac{1.6}{2.1}1.7}{\sqrt{\frac{1.6}{2.1}0.5}}\}\\
  &=
  \Phi(-1.288)\\
  &\approx
  0.0988\end{align}
  $$

(c) Show that for any $0 < s < t$, $B(t)$ and $B(s) - \frac{s}{t} B(t)$ are independent.

- **Solution:**    
  对于任意 $s\leq t$ 都有联合分布：  
  $\begin{bmatrix}
  B(s)\\
  B(t)\end{bmatrix} \sim N(\begin{bmatrix}
  0\\
  0\end{bmatrix},\begin{bmatrix}
  s& s\\
  s&t\end{bmatrix})$   

  因此我们有：  
  $$
  \begin{align}
  \begin{bmatrix}
  B(t)\\
  B(s)-\frac{s}{t}B(t)\end{bmatrix}
  &=
  \begin{bmatrix}
  0 & 1\\
  1 & -\frac{s}{t}\end{bmatrix}\begin{bmatrix}
  B(s)\\
  B(t)\end{bmatrix}\\
  &\overset{d}=
  N(\begin{bmatrix}
  0 & 1\\
  1 & -\frac{s}{t}\end{bmatrix}
  \begin{bmatrix}
  0\\
  0\end{bmatrix},
  \begin{bmatrix}
  0 & 1\\
  1 & -\frac{s}{t}\end{bmatrix}
  \begin{bmatrix}
  s& s\\
  s&t\end{bmatrix}
  \begin{bmatrix}
  0 & 1\\
  1 & -\frac{s}{t}\end{bmatrix}^T)\\
  &=
  N(\begin{bmatrix}
  0\\
  0\end{bmatrix},\begin{bmatrix}
  t&\\
  &s-\frac{s^2}{t}\end{bmatrix})\end{align}
  $$
  我们知道 $B(t)$ 和 $B(s)-\frac{s}{t}B(t)$ 联合正态，且不相关，因而相互独立.



### Problem 5

Let $\{B(t):t \geq 0\}$ be a standard Brownian motion and $0 < s < 1$.

**(a)** What is the distribution of $B(s) + B(s^2)$?

- **Solution:**    
  对于任意 $0 < s < 1$ 都有联合分布：  
  $\begin{bmatrix}
  B(s^2)\\
  B(s)\end{bmatrix} \sim N(\begin{bmatrix}
  0\\
  0\end{bmatrix},\begin{bmatrix}
  s^2& s^2\\
  s^2&s\end{bmatrix})$   

  因此我们有：
  $$
  \begin{align}
  B(s) + B(s^2) 
  &= 
  \begin{bmatrix}
  1\\
  1\end{bmatrix}^T
  \begin{bmatrix}
  B(s^2)\\
  B(s)\end{bmatrix}\\
  &=
  N(\begin{bmatrix}
  1\\
  1\end{bmatrix}^T\begin{bmatrix}
  0\\
  0\end{bmatrix},\begin{bmatrix}
  1\\
  1\end{bmatrix}^T \begin{bmatrix}
  s^2 & s^2\\
  s^2 & s\end{bmatrix}\begin{bmatrix}
  1\\
  1\end{bmatrix})\\
  &=
  N(0,3s^2+s)\end{align}
  $$

**(b)** What is the distribution of $B(s) + B(s^2) + B(1)$?

- **Solution:**    
  对于任意 $0 < s < 1$ 都有联合分布：  
  $\begin{bmatrix}
  B(s^2)\\
  B(s)\\
  B(1)\end{bmatrix} \sim N(\begin{bmatrix}
  0\\
  0\\
  0\end{bmatrix},\begin{bmatrix}
  s^2& s^2 & s^2\\
  s^2&s & s\\
  s^2 & s & 1\end{bmatrix})$   

  因此我们有：
  $$
  \begin{align}
  B(s) + B(s^2) + B(1)
  &= 
  \begin{bmatrix}
  1\\
  1\\
  1\end{bmatrix}^T
  \begin{bmatrix}
  B(s^2)\\
  B(s)\\
  B(1)\end{bmatrix}\\
  &=
  N(\begin{bmatrix}
  1\\
  1\\
  1\end{bmatrix}^T\begin{bmatrix}
  0\\
  0\\
  0\end{bmatrix},\begin{bmatrix}
  1\\
  1\\
  1\end{bmatrix}^T \begin{bmatrix}
  s^2 & s^2 & s^2\\
  s^2 & s & s\\
  s^2 & s & 1\end{bmatrix}\begin{bmatrix}
  1\\
  1\\
  1\end{bmatrix})\\
  &=
  N(0,5s^2 + 3s + 1)\end{align}
  $$

**(c)** What is the distribution of $B(s)$ given that $B(1) = 0$?

- **Solution:**  
  对于任意 $s\leq t$ 都有条件分布 $(B(s)|B(t)=y) \overset{d}= N(\frac{s}{t}y,\frac{s}{t}(t-s))$    
  因此对于任意 $0<s<1$ 都有条件分布:
  $$
  (B(s)|B(1)=0) \overset{d}= N(\frac{s}{1}0,\frac{s}{1}(1-s)) = N(0,s(1-s))
  $$

**(d)** Determine $\text{E}(B(s) B(s^2) | B(1) = 0)$.

- **Solution 1:**    
  应用全期望公式，我们有：
  $$
  \begin{align}
  \text{E}[B(s^2)B(s)|B(1)=0]
  &=
  \text{E}[\text{E}[B(s^2)B(s)|B(s)]|B(1)=0]\\
  &=
  \text{E}[B(s)\cdot \text{E}[B(s^2)|B(s)]|B(1)=0]\\
  &=
  \text{E}[B(s)\cdot \text{E}[N(\frac{s^2}{s}B(s),\frac{s^2}{s}(s-s^2))]|B(1)=0]\\
  &=
  \text{E}[B(s)\cdot sB(s)|B(1)=0]\\
  &=
  s\cdot \text{E}[(B(s))^2 |B(1)=0]\\
  &=
  s\cdot (\text{Var}(B(s)|B(1)=0) + (\text{E}[B(s)|B(1)=0])^2)\\
  &=
  s\cdot (s(1-s) + 0^2)\quad (B(s)|B(1)=0 \overset{d}=N(0,s(1-s)))\\
  &=
  s^2(1-s)\end{align}
  $$

- **Solution 2:**

  - **Lemma:**  
    $\text{Cov}(X,Y|Z=z) = \text{Cov}(X,Y) - \frac{\text{Cov}(X,Z)\text{Cov}(Y,Z)}{\text{Var}(Z)}\ \ (\forall\ z)$  

  根据引理我们有：  
  $$
  \begin{align}
  \text{E}(B(s) B(s^2) | B(1) = 0)
  &=
  \text{Cov}(B(s),B(s^2)|B(1) = 0) + \text{E}[B(s)|B(1)=0] \cdot \text{E}[B(s^2)|B(1)=0]\\
  &=
  \text{Cov}(B(s),B(s^2)) - \frac{\text{Cov}(B(s),B(1))\cdot\text{Cov}(B(s^2),B(1))}{\text{Var}(B(1))} + \text{E}[N(\frac{s}{1}0,\frac{s}{1}(1-s))]\cdot 
  \text{E}[N(\frac{s^2}{1}0,\frac{s^2}{1}(1-s^2))]\\
  &=
  s \and s^2 - \frac{(s\and 1)\cdot (s^2\and 1)}{1} + 0\cdot 0\\
  &=
  s^2 - s^3\end{align}
  $$



### Problem 6

Let $\{B(t):t \geq 0\}$ be a standard Brownian motion.

(a) Let $Z(t) = |B(t)|$. Determine the probability density function of $Z(t)$.

- **Solution:**  
  给定 $t>0$，$Z(t)$ 的概率密度函数为：
  $$
  \begin{align}
  \text{P}\{Z(t) = z\} 
  &= \text{P}\{|B(t)| = z\}\\
  &= 2\text{P}\{B(t) = z\}\\
  &= 2\cdot \text{P}\{N(0,t)=z\}\\
  &= 2\cdot \frac{1}{\sqrt{2\pi t}}e^{-\frac{x^2}{2t}}\\
  &= \sqrt{\frac{2}{\pi t}}e^{-\frac{x^2}{2t}}
  \end{align}
  $$

(b) Let $T_a$ be the first time $B(t)$ hits $a$, where $a > 0$,   
and $Y(t)$ be the process absorbed at $a$, i.e. $Y(t) =
\begin{cases}
B(t) & \text{if } t < T_a \\
a & \text{if } t \geq T_a
\end{cases}$ 

Determine the cumulative distribution function of $Y(t)$.

- **Lemma: Reflect Principle**  
  对于任意 $\begin{cases}
  a\in\R\\
  t>0\end{cases}$，都有 $\text{P}\{B(t) \geq a\} = \text{P}\{B(t)\leq -a\}$ 成立.  
  **推论：**  
  - $\text{P}\{T_a\leq t\} = \text{P}\{\underset{0\leq s\leq t}{\max}B(s) \geq a\} = 2\text{P}\{B(t)\geq a\} = 2(1-\Phi(\frac{a}{\sqrt{t}}))$   
    总之对于 $T_a\ (a\neq 0)$ 我们有 $\text{P}\{T_a\leq t\} = 2(1-\Phi(\frac{|a|}{\sqrt{t}}))$ 成立. 
  - $\tilde B(t) \overset{\Delta}= \begin{cases}
    B(t) & t\leq T_a\\
    2B(T_a)-B(t) & t> T_a\end{cases})$ 是一个标准 Brown 运动
  

**Solution:**    
考虑 $a>0$ 的情况：

- 当 $y<a$ 时，我们有：
  $$
  \begin{align}
  \text{P}\{Y(t)\leq y\} 
  &=
  \text{P}\{B(t)\leq y,\ T_a>t\}\\
  &=
  \text{P}\{B(t)\leq y,\ \max_{0\leq s\leq t}B(s)<a\}\\
  &=
  \text{P}\{B(t)\leq y\} - \text{P}\{B(t)\leq y,\ \max_{0\leq s\leq t}B(s)\geq a\}\\
  &=
  \text{P}\{B(t)\leq y\} - \text{P}\{\tilde B(t)\geq 2a-y\}\quad (\tilde B(t) \overset{\Delta}= \begin{cases}
  B(t) & t\leq T_a\\
  2B(T_a)-B(t) & t> T_a\end{cases})\\
  &=
  \Phi(\frac{y}{\sqrt{t}}) - (1-\Phi(\frac{2a-y}{\sqrt{t}}))\\
  &=
  \Phi(\frac{y}{\sqrt{t}}) - 1 +\Phi(\frac{2a-y}{\sqrt{t}})\end{align}
  $$

- 当 $y\geq a$ 时，我们有：  
  $$
  \begin{align}
  \text{P}\{Y(t)\leq y\} 
  &=
  1\end{align}
  $$

因此对于 $a>0$ 的情况 $\text{P}\{Y(t)\leq y\} = \begin{cases}
\Phi(\frac{y}{\sqrt{t}}) - 1 +\Phi(\frac{2a-y}{\sqrt{t}}) &\text{if }y<a\\
1 &\text{if }y\geq a\end{cases}$     

****

对称地，对于 $a<0$ 的情况：  
$\begin{align}
\text{P}\{Y(t)\geq y\} 
&= \begin{cases}
(1-\Phi(\frac{y}{\sqrt{t}})) - 1 + (1-\Phi(\frac{2a-y}{\sqrt{t}})) &\text{if }y> a\\
1 & \text{if }y\leq a\end{cases}\\
&=
\begin{cases}
-\Phi(\frac{y}{\sqrt{t}}) + 1 -\Phi(\frac{2a-y}{\sqrt{t}}) &\text{if }y> a\\
1 & \text{if }y\leq a\end{cases}\end{align}$ 

因此 $\text{P}\{Y(t) < y\}=\begin{cases}
0 &\text{if }y\leq a\\
\Phi(\frac{y}{\sqrt{t}})  +\Phi(\frac{2a-y}{\sqrt{t}}) &\text{if }y>a\end{cases}$

考虑到 $\text{P}\{Y(t) = a\} = \text{P}\{T_a\leq t\} = 2(1-\Phi(\frac{|a|}{\sqrt{t}})) =2(1-\Phi(\frac{-a}{\sqrt{t}}))=2\Phi(\frac{a}{\sqrt{t}})$    
我们有 $\text{P}\{Y(t) \leq y\}=\begin{cases}
0 &\text{if }y< a\\
 2\Phi(\frac{a}{\sqrt{t}})&\text{if }y=a\\
\Phi(\frac{y}{\sqrt{t}})  +\Phi(\frac{2a-y}{\sqrt{t}}) &\text{if }y>a\end{cases} = \begin{cases}
0 & \text{if }y<a\\
\Phi(\frac{y}{\sqrt{t}})  +\Phi(\frac{2a-y}{\sqrt{t}}) &\text{if }y\geq a\end{cases}$     
